# homelab-portainer — Environment Variables
#
# Portainer CE requires no runtime environment variables for this deployment.
# Admin credentials are set interactively via the web UI on first launch.
#
# This file exists as standardized program scaffolding per STANDARDS.md.
# A local .env file is optional, is not required to run this service,
# and must never be committed.