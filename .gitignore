# Environment variables — never commit
.env

# Logs
*.log

# macOS system files
.DS_Store

# Editor swap and backup files
*.swp
*.bak

# Runtime data and local backups
/data/
/backups/

# Node — tooling only, not application code
node_modules/
yarn-error.log
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# Prettier cache
.prettier-cache

# Backups — local only, never commit
backups/